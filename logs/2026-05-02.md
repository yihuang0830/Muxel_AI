# 2026-05-02 开发日志

## 今天做了什么

### 1. Step 2 歌单页面持续优化

- **备选歌单**：`generate()` 用 Last.fm 剩余候选（按热度排序，最多 20 首）自动填充备选列表，每首右侧有 ＋ 按钮加入正式歌单
- **Last.fm 状态可视化**：主题 caption 显示 `Last.fm ✅ N 首候选（tag1、tag2）` 或 `Last.fm 未启用`，方便确认数据库是否生效
- **表格与小张对话同步**：修复模糊匹配（去书名号/标点/空格），修复 `max_tokens` 不足导致 JSON 截断的问题，让排序/删除指令能正确更新左边表格

### 2. Step 3：主持稿 + 主持人选择

- `config/hosts.json`：两位主持人配置
  - 小像（男）：温暖随性，理工男气质，幽默风趣，口语化，有自己的具体反应
  - 素素（女）：占位，标注"即将推出"，disabled
- `pipeline/step3_script.py`：
  - `generate()`：小像根据选题方向 + 歌单写完整中文主持稿
  - `chat_reply()`：导演给反馈，小像修改并返回完整新稿
- Step 3 UI：
  - 第一次进入显示主持人选择卡片（选完写入 STATUS.json）
  - 生成稿本后：左边可编辑稿子，右边和小像实时对话修改

### 3. 步骤间自由导航

- 进度条中已完成的步骤（✅）变为可点击按钮，点击任意已完成步骤可回跳，数据文件保留不重置

## 下一步

- Step 7：上传文案（B站/YouTube 标题/简介/标签）
- Step 4：封面生成（DALL-E 3 或其他图像 API）
